// &&运算符和||运算符的使用
//&&运算符 - 逻辑运算符 - 判断两个表达式的真假性，只有两个表达式同时为真才为真，有一个为假则为假，具有短路性质。
//&运算符  - 位运算符   - 将两个二进制的数逐位相与，结果是相与之后的结果。
//1.
//#include<stdio.h>
//int main()
//{
//	int i = 0, a = 0,b = 2, c = 3, d = 4;
//	i = a++ && ++b && d++;
//	//i=a++||++b||d++;
//	printf(" a=%d\n b=%d\n c=%d\n d=%d\n",a,b,c,d);
//	return 0;
//}
//程序输出的结果是什么？
//1 2 3 4
//总结：当多个表达式参与"&&"运算时，若第一个表达式值为0，则后面的表达式不再参与运算。整个表达式的结果为0(假);
//2.
#include<stdio.h>
int main()
{
	int i = 0, a = 1, b = 2, c = 3, d = 4;
	i=a++||++b||d++;
	printf(" a=%d\n b=%d\n c=%d\n d=%d\n", a, b, c, d);
	return 0;
}
//程序输出的结果是什么？
//2 2 3 4
//总结：当多个表达式参与"||"运算时，若第一个表达式值为1，则后面的表达式不再参与运算，整个表达式的结果为1(真);
